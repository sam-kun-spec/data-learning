"""
1. What is Pandas?
NumPy = works with numbers only. Pandas = works with tables (rows + columns) — like Excel but in Python.

Installation:
pip install pandas


Why Pandas?
Excel      → opens CSV, manual work
SQL        → queries a database
Pandas     → does BOTH in Python, automated
Real DA work = 70% Pandas. It's the most important library you'll learn.
"""

import pandas as pd
print(pd.__version__)