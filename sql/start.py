# basic info about SQL:-
"""
What is SQL?

SQL = Structured Query Language

Used to talk to databases. You store data in tables,
SQL lets you — fetch, filter, insert, update, delete that data.
You already know pandas — SQL does the same things but on databases, not DataFrames.

"""

#installation of SQL:-
"""
Installation — what you need
You need 2 things:
1. PostgreSQL — the actual database (free)
Download: postgresql.org/download → pick Windows/Mac/Linux
2. pgAdmin — visual interface to write + run SQL (comes with PostgreSQL)

After install
pgAdmin opens in browser → you create a database → create tables → start writing SQL.
"""

